<h1>Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %> - <%= link_to 'Delete', list_path(list), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to '+ New list', new_list_path %>
